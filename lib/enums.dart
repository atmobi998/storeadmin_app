enum MenuState { home, favourite, message, profile }
enum StoreMenuState { logout,
    home, back, delstore, 
    addstore, editstore, 
    payedit, savestore, storelist, 
    category, editcat, addcat, delcat,
    product, editprod, addprod, delprod,
    salepos, editpos, addpos, delpos,
    profile, none 
}
